apiVersion: v1
kind: ConfigMap
metadata:
  name: tracer-collector-config
  namespace: tracer-system
data:
  HTTP_ADDR: ":8080"
  DB_PATH: "/data/tracer.db"
  RETENTION_DAYS: "10"
  TRACE_SEARCH_WINDOW_DAYS: "2"
  SQLITE_CACHE_KB: "16000"
  INGEST_CHAN_CAPACITY: "5000"
  WRITER_FLUSH_MS: "200"
  WRITER_FLUSH_SIZE: "500"
